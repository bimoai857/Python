# import mod1 #Doesnot add the names
            #of the mod1 in this namespace
            #but only imports the module name

            #runs the executable line of code(function calls)
# mod1.fib(100)
# print(__name__)

# from mod1 import * #This add all the function and variable names in this
                   #this namespace
# fib(1000)

#we can use as keyword to define an alias


# print(dir())   #dir() is used to find out names a module defines

#Packages are collection of modules.